# Daily leaderboard: execution notes

The ledger from building the leaderboard on 2026-09-21, kept because it is the only record of two things the code cannot show: the decisions taken when the plan turned out to be wrong, and the findings that were deliberately not fixed.

Lines beginning `Ruling:` are decisions made during execution, several of them overriding the plan. Lines containing `minor (deferred)` are review findings consciously left alone, each with its reason — treat that as the follow-up list. The plan is `2026-09-21-daily-leaderboard-web.md`, the design is in `../specs/`.

# SDD ledger — plan: docs/superpowers/plans/2026-09-21-daily-leaderboard-web.md

Spec: docs/superpowers/specs/2026-09-21-daily-leaderboard-design.md (read, reachable)
Worktree: C:/dev/puzzle-hustle/.worktrees/leaderboard on feat/leaderboard
Baseline: 124 core tests pass at 006b0cc

## Pre-flight scan

### Pairs sharing a file or an interface

| Tasks | Produced vs consumed | Finding |
|---|---|---|
| 1 → 7 | `parsePuzzleId`, `periodEndsAt`, `minimumSeconds` | Names and signatures match; T7 also imports `periodKey` straight from core, which exists today |
| 1 → 14 | `parsePuzzleId` filters non-period ids in the queue | Consistent |
| 2 → 3..9 | `Env`, `json`, `error`, `cors`, `src/index.ts`, migrations | All later tasks extend the same router; `Env` gained `SESSION_LIMIT?` in T2, used only in T5 |
| 3 → 5 | `signSession`, `verifySession` | Consistent; T5's test imports `signSession` for the stale-token case |
| 4 → 5 | `verifyGoogleIdToken(token, {audiences})` | Consistent |
| 5 → 6,7,8,9 | `requirePlayer` gate placed above every later route | Consistent; gate ordering is stated explicitly in T5 step 5 |
| 6 → 9 | `leaveGroup` reused by `DELETE /account` | Consistent; import already present from T6 |
| 6 → 12 | group row `{id, code, name, members, owner}` | Identical shape in `GroupRow` and the web `Group` interface |
| 7 → 14 | status strings `stored\|duplicate\|rejected\|expired\|throttled` | Queue drops all but `throttled`; matches |
| 8 → 15 | `BoardData` shape, `me` 1-based, `percentile {total, faster}` | Consistent |
| 10 → 11,12,14,15 | `apiFetch`, `readSession`, `writeSession`, `Session` | Consistent |
| 11 → 12,15 | `useSession` | Consistent |
| 12 → 13,15 | `Friends` page, `useGroups` exported from the page file | Consistent; T15 and Daily.tsx both import `useGroups` from `pages/Friends.tsx` |
| 14 → 15 | queue flush runs before the placement fetch | Consistent; T15 states the offline case renders nothing |
| shared file `src/index.ts` | T2,5,6,7,8,9 | Sequential, one task at a time, no overlap |
| shared file `theme.css` | T11,12,15 | Additive class blocks only, no rewrites |
| shared file `Play.tsx` | T14 (enqueue), T15 (placement line) | Different hunks, T15 depends on T14 |
| shared file `App.tsx` | T12 (`/friends`), T13 (`/join`) | Adjacent branches, T13 after T12 |

### Each task against itself

| Task | Finding |
|---|---|
| 1 | Tests and code agree. `periodEndsAt` expectations are written for CEST (UTC+2); step 6 says so and tells the implementer to fix the expectation, not the code |
| 2 | Agrees. `database_id = "local"` is a placeholder with a stated replacement point in T16 |
| 3 | Agrees |
| 4 | Agrees. Test generates a real RSA key pair in workerd rather than stubbing crypto |
| 5 | **Risk:** tests use `vi.spyOn(google, 'verifyGoogleIdToken')` on an ESM export. See ruling below |
| 6 | Agrees. `MAX_GROUPS` is checked before the duplicate-join check, so a player already in five groups rejoining one gets `limit` rather than `already`. Harmless, both are refusals |
| 7 | Agrees after the future-period fix from the plan self-review |
| 8 | Agrees. `SUM(...)` over zero rows yields null and is defaulted to 0 |
| 9 | Agrees |
| 10 | Agrees |
| 11 | **Defect:** the GSI callback is `async` and nothing catches a failing `apiFetch`. See ruling below |
| 12 | Agrees |
| 13 | Agrees |
| 14 | Agrees |
| 15 | Agrees |
| 16 | Requires creating real Cloudflare resources. See ruling below |

## Rulings

Ruling: Task 5 and 6 tests may use `vi.mock('../src/google.ts', ...)` instead of `vi.spyOn` if spying on the ESM export throws "cannot redefine property" under the workers pool — the behaviour under test is the route, not the spy mechanism. Reason: `vi.spyOn` on ESM exports depends on Vite's module instrumentation and is not guaranteed in this pool. Cost if wrong: none, both mechanisms assert the same behaviour; only the test file changes.

Ruling: Task 11's sign-in callback must wrap its `apiFetch` in try/catch and surface a failure through the existing `toast()`, rather than leaving an unhandled rejection. Reason: a sign-in that silently does nothing is exactly the failure mode the offline rules forbid elsewhere in this app. Cost if wrong: a few extra lines in `auth.ts`.

Ruling: Task 11's browser verification step cannot run without a real Google OAuth client id, which does not exist yet. The implementer verifies the no-client-id path (card renders, no crash) and records the signed-in path as unverified. Reason: the code path is written either way and the plan orders Task 11 before the credential exists. Cost if wrong: a sign-in bug survives until Moritz supplies a client id and tries it.

Ruling: Task 16 stops and goes to Moritz instead of being dispatched. Reason: it creates a D1 database, sets a production secret and deploys to a live subdomain — side effects outside this worktree, which this process does not take on its own. Cost if wrong: none, the API simply is not deployed until he runs it.

## Progress
Task 1: implemented (commits 006b0cc..a874855, 134 tests pass per report), task review dispatched
Task 1: review 1 — spec compliant, 1 Important (plan-mandated), 2 minors. Controller resolved the reviewer's one cannot-verify item: commit body is exactly the required sentence, no trailers, 5 files staged explicitly.
Task 1: Ruling: the weekly ISO-week gap is real and gets fixed, although my own plan text mandated the flawed code. Reason: dateInPeriod accepts 2025-W53 although 2025 has 52 ISO weeks, and the spec makes key validity this module's whole job; a score could be stored under a key no puzzle ever had. schedule.ts already exports isoWeek, so the fix is a roundtrip check, not new date maths. Cost if wrong: a few lines and one extra test in a module nothing consumes yet.
Task 1: minor (deferred): the implementer's self-review claimed edge-case coverage it did not have (week 53 in a 52-week year).
Task 1: minor (deferred): PERIOD_FACTOR 1/1.4/2 in plausibility.ts has no stated basis; one line of rationale would help later tuning.
Task 1: fix round 1/5 (1 addressed, 0 open; commits a874855..dfa273e)
Task 1: minor (deferred): no positive assertion that 2026-W53 still parses. The code was hand-verified to accept it, but the 53-week boundary is the case an over-corrected fix would break. One line in packages/core/test/puzzleId.test.ts. Final review should triage it.
Task 1: complete (commits 006b0cc..dfa273e, review clean)
Controller: plan reference code for Task 1 corrected to match the shipped fix (commit dae6903, docs only).
Task 2: implemented DONE_WITH_CONCERNS (commit 44eaca9), three deviations reported. Controller checked the registry before review.
Task 2: Ruling: a local Vitest pin in apps/api is unavoidable — pool 0.22.0 requires vitest ^4.1 and 0.12.21 requires 2.0.x-3.2.x, so no release supports the root's Vitest 5. But the pin goes to the newest supported pairing (pool ^0.22 + vitest ^4.1), not to pool 0.12.21 + vitest 3.2.7. Reason: the repo rule is newest versions, and seven more tasks build their tests on this harness; migrating three tests now is the cheapest this ever gets. Fallback if defineWorkersConfig/readD1Migrations do not exist in 0.22: keep the old pairing and document the check. Cost if wrong: one fix round on a config file.
Task 2: Ruling: apps/api/tsconfig.json extends tsconfig.base.json like the other two packages instead of standing alone. Reason: my brief invented a standalone config against house style, and the base already sets skipLibCheck, so the deviation I forbade was really my own brief being wrong. Side effect accepted: the base adds noUncheckedIndexedAccess and exactOptionalPropertyTypes, making the package stricter. Cost if wrong: type errors surface in http.ts/index.ts and must be fixed rather than configured away.
Task 2: test/env.d.ts accepted as a necessary ProvidedEnv augmentation beyond the brief's file list.
Task 2: fix pass landed pool 0.22.0 + vitest 4.1.11; defineWorkersConfig does not exist in 0.22 (replaced by a cloudflareTest Vite plugin), readD1Migrations moved to the package root, singleWorker dropped with no replacement.
Task 2: per-file D1 isolation is fine by design — every API test file applies migrations in beforeAll and clears tables in beforeEach, and none may rely on another file's data. Recorded in the plan so Tasks 3-9 inherit it.
Controller: plan's Task 2 install line, tsconfig and vitest.config blocks corrected to the shipped versions (docs only).
Task 2: review 1 — spec compliant, Approved, 0 Critical/Important, 2 minors. Controller resolved both cannot-verify items: the env.d.ts ruling was genuinely mine (given in the fix-pass message), and the registry confirms wrangler 4.136.0 and @cloudflare/workers-types 5.20260921.1, both inside the installed caret ranges, so the newest-versions constraint holds.
Task 2: minor (deferred): apps/api/tsconfig.json writes "lib": ["es2022"] in lowercase while apps/web uses "ES2024" casing. No functional effect, monorepo consistency only.
Task 2: minor (deferred): per-file D1 storage isolation under pool 0.22 is unverified beyond a single test file. Task 5 adds two more test files and will exercise it immediately, so this resolves itself there rather than needing a separate check.
Task 2: complete (commits dae6903..57fb696, review clean)
Task 3: implemented (commit ed44b66, 5 token tests pass per report), task review dispatched
Task 3: review 1 — token.ts itself clean (verify-before-trust, crypto.subtle.verify not string compare, correct expiry boundary, safe base64url, minimal payload). 2 Important, 1 minor. Both Importants are process, not code.
Task 3: Ruling: commits keep the mandated sentence "Implemented with assistance from Claude Opus 5." even though the Haiku subagent wrote "Claude Haiku 4.5." and is arguably more literally correct under Moritz's global.md rule "Claude <Modell>". Reason: one branch, one attribution line; fifteen tasks across three model tiers would otherwise leave a patchwork history, and the line exists to record that Claude assisted, not which tier ran which task. Cost if wrong: Moritz prefers per-model accuracy and the branch carries a slightly coarse attribution. Surfaced to him at the end so he can decide the convention.
Task 3: Ruling: the exp-equals-now boundary test (Minor) was folded into the fix round rather than deferred. Reason: a round was already running for the two Importants, so it extended nothing, and it guards a security-relevant comparison against a later edit. Cost if wrong: one extra assertion nobody needed.
Task 3: fix round 1/5 (3 addressed, 0 open; commit ed44b66 amended to df453a1). Controller independently verified the amended commit body before dispatching the re-review.
Task 3: complete (commits 4c25451..df453a1, review clean)
Task 4: implemented (commit 5a93e8c, 5 google tests pass per report; controller verified commit body and file scope), task review dispatched
Task 4: review 1 — spec compliant, Approved. google.ts itself verified clean on every security check: all five checks present and unskippable, algorithm hardcoded on importKey so the header cannot steer it, unknown kid rejected with no fallback key, signature verified before sub is returned, null on every failure path, no console output, email never read, and a failed JWKS fetch is NOT cached (the same class of bug this project already hit in ads.ts).
Task 4: 1 Important (report cites brief line numbers, not shipped file lines; the logic it describes is correct), 1 minor (no test for the unknown-kid path).
Task 4: open item for the final review and for the deploy hand-off: the production googleJwks() fetch path is exercised by no test by design, so the real Google certs response shape is unconfirmed until a live sign-in runs.
Task 4: fix round 1/5 (2 addressed, 1 new Important from the fix itself; commits 5a93e8c..d1db2d3). The new unknown-kid test serves a key set from a second, unrelated key pair, so a fallback-to-first-key regression would still fail signature verification and the test would keep passing. It guards nothing. Cause is my own fix instruction, which said to take the key set from a second issue() call.
Task 4: fix round 2/5 (1 addressed, 0 open; commit 52e42eb). Re-reviewer independently traced the key material and confirmed the test now fails under a fallback-to-first-key regression and passes today via the kid guard.
Task 4: complete (commits df453a1..52e42eb, review clean)
Task 5: implemented DONE_WITH_CONCERNS (commit 542b99d, 27 tests across 5 files). Controller inspected both concerns directly before review.
Task 5: Ruling: the implementer's correction to my names.ts snippet stands. My version collapsed all whitespace before the character check, so "bad\nname" became "bad name" and passed, contradicting the brief's own test. Collapsing only literal spaces leaves control characters in place to be rejected. Cost if wrong: none, the stricter behaviour is what the test always demanded.
Task 5: Ruling: the router keeps the gate above the 404 fallback, so an unauthenticated request to an unknown path gets 401 rather than 404, and the pre-existing health test was split to assert both. Reason: everything below the gate being authenticated by construction is the property worth having, since no later route can forget it; an alternative that 404s first would need a second list of known routes and reintroduce exactly that risk. Not leaking the route table to anonymous callers is a small bonus. Cost if wrong: a slightly unconventional 401-before-404, easy to change in one place.
Controller: plan's names.ts snippet corrected to the shipped version.
Task 5: review 1 — spec compliant, 1 Important, 2 minors. Controller resolved the cannot-verify item by running the suite directly: 5 files, 27 tests, all pass on vitest 4.1.11. That also closes the Task 2 question about per-file D1 isolation, which held across five files without intervention.
Task 5: minor (deferred): a name offered at POST /session that fails validation is silently replaced by a generated one, so the client never learns it was rejected. Brief-specified behaviour; closing it means changing the /session response contract, which Tasks 10 and 11 consume. Final review should triage.
Task 5: fix round 1/5 (1 addressed, 1 partially; commits 542b99d..821993b). The implementer honestly flagged that its new race test never reaches the try/catch: the leading SELECT hits and returns first, so the added branch was untested and, through the public function, untestable.
Task 5: Ruling: rather than write a cleverer test, upsertPlayer becomes INSERT ... ON CONFLICT DO NOTHING RETURNING with a SELECT fallback, so the database resolves the race atomically and both branches are reachable by simply calling the function twice. Reason: an untestable defensive branch in the identity path is worse than the race it guards, and the rewrite also drops a round trip and removes exception-as-control-flow. Cost if wrong: D1's SQLite may not accept the upsert or RETURNING, in which case the fallback is the current try/catch with the untested branch recorded honestly — the implementer was told to stop and ask rather than improvise.
Controller note: rounds 1 and 2 were verified by a single scoped re-review over 542b99d..c8f787d, because round 1 went straight into round 2 on the implementer own flag that its race test never reached the new branch. The fix surface reviewed is the same either way.
Task 5: fix round 2/5 (2 addressed, 0 open; commits 542b99d..c8f787d). Re-reviewer independently confirmed both branches of the rewritten upsertPlayer are forced by the test against the real D1, and that the feature check by behaviour is sound: no path exists to a passing test unless ON CONFLICT DO NOTHING and RETURNING both work.
Task 5: minor (deferred): every sign-in now issues a no-op write instead of a pure read. Inert for correctness, unmeasured against D1 write quota. Final review should triage.
Task 5: complete (commits 52e42eb..c8f787d, review clean)
Controller: plan's upsertPlayer snippet corrected to the shipped atomic version.
Task 6: implemented DONE_WITH_CONCERNS (commit f28495e, 36 tests across 6 files). Both concerns are the ones my pre-flight scan predicted for this task, both from my brief, both reported honestly rather than papered over.
Task 6: Ruling: joinGroup reorders its checks from most specific to most general (group exists, already a member, group full, caller under cap) with an explicit membership read, keeping the primary key as the backstop for a genuine race. Reason: a player in five groups re-opening an invite for a group they are already in was told "you are in five groups already", which is true but is not why the request failed and does not tell them what to do. Cost if wrong: one extra read per join.
Task 6: Ruling: the ownership handover becomes ORDER BY joined_at, player_id. Reason: not fairness, repeatability — an unrepeatable outcome in a data path is a bad thing to leave for someone to debug later. Cost if wrong: none; the tiebreak is arbitrary either way, this one is at least the same every time.
Task 6: review 1 — spec compliant, Approved, 2 Important (both plan-mandated), 2 minors. Access-control surface traced route by route and found sound: listGroups leaks nothing, leave only ever deletes the caller's own row, remove is owner-only and cannot remove the owner, and no route leaves a group ownerless.
Task 6: Ruling: the member and group caps stay check-then-insert with no database-level backstop, i.e. two simultaneous joins at the boundary can overshoot by one. Reason: these are product guardrails against a group becoming a public board, not security boundaries, and overshooting 50 members or 5 groups by one harms nothing. The atomic version needs a compound conditional INSERT whose failure mode (the insert silently did nothing) is harder to reason about than the current code. The "already a member" case did get a backstop because a duplicate membership throws on the primary key; an overshooting cap does not throw, it just counts one too far. Cost if wrong: a group could hold 51 members. Surfaced to Moritz.
Task 6: fix round 2/5 (2 addressed, 0 open; commits c486372..d7bd460). The implementer verified D1 batch atomicity with a throwaway probe against the real harness rather than trusting documentation, and left nothing of it behind.
Task 6: minor (deferred): the member count in joinGroup's success payload is read before the insert, so a concurrent join can make it off by one in that one response. The next listGroups read is always correct.
Task 6: complete (commits 9891fd6..d7bd460, review clean)
Task 7: implemented DONE_WITH_CONCERNS (commit 55c7091, 46 tests across 7 files). One reported deviation from the brief's verbatim code.
Task 7: Ruling: an explicit existing-row read runs before judge(), so a second submission of an already-stored puzzle is called duplicate even when its time is implausible. Reason: my brief's own code failed my brief's own first test — judge() rejected on the seconds floor before anything could detect the existing row. Duplicate is also the honest answer: the reason the submission had no effect is that one already exists, and the plausibility of a value that will be ignored anyway is beside the point. The insert's failure stays as the race backstop. Cost if wrong: one extra read per submitted entry.
Task 7: review 1 — spec compliant, Approved, 0 Critical/Important, 4 minors. Reviewer confirmed the deviation is the minimal fix and that it incidentally cured a second latent bug: under the brief's literal code an already-stored puzzle submitted after the daily budget ran out would have come back 'throttled' instead of 'duplicate'. Also confirmed no client timestamp feeds any decision, created_at is the server clock, and budget is decremented only after a confirmed insert.
Task 7: controller verified the commit body and file scope directly (the reviewer could not see the message body from the diff).
Task 7: minor (deferred, LOAD-BEARING FOR TASK 14): an entry whose puzzle field is not a string comes back with puzzle: '' rather than a stringified identifier. The queue in Task 14 matches results to entries by puzzle value, not by position, so such an entry would never be removed from the queue. Our own client cannot produce one (enqueue is typed and filters through parsePuzzleId), but Task 14's dispatch must carry this or the failure mode is a queue that never empties.
Task 7: minor (deferred): no test covers the daily budget running out mid-batch with several distinct valid entries; the existing test submits one entry after the budget is already gone.
Task 7: minor (deferred): the existing-row SELECT runs even for entries whose id could never have been stored, and the malformed-entry line would read better as a small named helper.
Task 7: complete (commits d7bd460..55c7091, review clean)
Task 8: implemented (commit 0b9bc93, 50 tests across 8 files; controller verified commit body and file scope), task review dispatched
Task 8: review 1 — spec compliant, Approved, 0 Critical/Important, 3 minors. Reviewer confirmed the membership gate precedes every other query and that a non-existent group and a group the caller is not in produce the identical answer, and specifically checked the numbered-placeholder binding in the percentile statement, which is the shape two earlier tasks got wrong. This reference code was correct as written.
Task 8: the reviewer's cannot-verify item was the commit body, which the controller had already checked before dispatch: subject imperative, body exactly the required sentence, no trailer, three files staged.
Task 8: minor (deferred): ORDER BY hints, seconds, created_at has no further tiebreak, and submitScores stamps one created_at per call, so two players in the same batch could tie all three. Deterministic in practice, untested.
Task 8: minor (deferred): the not-a-member test uses an existing group the caller is not in, never a bogus group id, so the indistinguishability property is inferred from code rather than asserted.
Task 8: complete (commits 55c7091..0b9bc93, review clean)
Task 9: implemented DONE_WITH_CONCERNS (commit 3f47de5, 52 tests across 9 files).
Task 9: Ruling: DELETE /account stays non-transactional across its per-group leaveGroup loop and the final batch. Reason: D1 has no interactive transactions, and leaveGroup needs reads between writes (is the group now empty, who is the longest-standing member) so it cannot collapse into one batch. The failure mode is already the benign one, because the player row is deleted last: a mid-loop failure leaves the account intact and retryable rather than half-erased and unreachable. Cost if wrong: a player who hits a database failure mid-deletion must tap delete again.
Task 9: review 1 — spec compliant, Approved, 0 Critical/Important, 1 minor. Reviewer enumerated every table and column that can name a player and confirmed each is cleared, including reports where the deleted player was the reporter rather than the target, and verified from the code that the player row really is deleted last.
Task 9: minor (deferred): no automated test covers an owner deleting their account while other members remain, so the ownership handover in that path is verified by reading leaveGroup rather than by a test.
Task 9: complete (commits 0b9bc93..568d3e4, review clean)
API complete: tasks 1-9, 54 tests across 9 files, all reviewed.
Task 10: implemented DONE_WITH_CONCERNS (commit ebc51de, 5 web tests, first tests apps/web has ever had). Three concerns raised, all three accepted for fixing before review.
Task 10: Ruling: signing out must reach the native backup. writeSession(null) removed the key without calling scheduleBackup, so the Preferences snapshot kept the old token and restoreBackup would write it back after a wipe, reinstall or device restore — a player who signed out could come back signed in. Fixed by adding removeSetting to storage.ts as the mirror of writeSetting, rather than patching the one call site, since the offline queue will want it too. Cost if wrong: one more helper in storage.ts.
Task 10: Ruling: an unparseable body on a 2xx now throws ApiError('bad_response') instead of resolving to {}. Reason: the caller is typed as receiving T and would silently get an object with every field missing — sign-in would store a token of undefined, the board would render as empty. A silent wrong answer is worse than a failure. The tolerant parse stays on the error path, where a missing body is legitimate. Cost if wrong: a broken proxy turns a blank screen into a caught error.
Task 10: Ruling: apps/web's tsconfig now includes the test directory, matching apps/api. It was harmless while the package had no tests; this task is what changed that.
Task 10: review 1 — spec compliant, Approved, 0 Critical/Important, 2 minors. Reviewer traced every failure surface and confirmed apiFetch can only ever reject with an ApiError, that the parsed-boolean cannot be fooled by a legitimate empty success body, and that the fetch-mock typing fix is a real fix rather than a cast that silences the checker.
Task 10: minor (deferred): clearProgress and resetProgress still call scheduleBackup unconditionally, duplicating removeSetting without its isBackedUp gate. Correct today, a cleanup candidate.
Task 10: minor (deferred): a 2xx body that parses as an array or a primitive is still returned via `data as T` with no runtime shape check.
Task 10: complete (commits 568d3e4..4a1a277, review clean)
Controller: merged main into feat/leaderboard before Task 11, because main had moved two commits and touched Profile.tsx, App.tsx, theme.css, package.json and the lockfile — exactly the files Tasks 11 to 15 need. Auto-merged with no conflicts; full suite green afterwards (core 134, web 7, api 54) and both typechecks clean. Merge commit is the new base for Task 11.
Cross-session note (hub-9f, investigating the in-puzzle clock): any change from that side would only add a pause source, never change the measurement method, so measured durations can only get shorter. That is the direction the plausibility floors in packages/core/src/plausibility.ts are exposed to — a floor that sits too close to a real time would start rejecting genuine solves. Current daily floors are 4 to 20 seconds against realistic times of a minute or more, so the margin is wide; re-check if the floors are ever tuned upward from real data.
Task 11: implemented DONE_WITH_CONCERNS (commit cba5be4). Verified in headless Chromium that the signed-out Profile tab renders in both themes with no client id and makes zero external requests.
Task 11: implementer corrected three things in the brief on its own, all right: the unhandled rejection in Google's callback, a className that no longer exists in theme.css (card -> card-lg), and a footer sentence promising sign-in "later" that the new card made false.
Task 11: Ruling: a rename the server rejects must be visible. The brief swallowed it, leaving the profile field and the Friends card showing two different names with no feedback — and the server's name is the one other players see in a group, so the screen was lying about the thing that matters. Once signed in the server's name is the truth: the three name_* rejections toast a specific reason and revert both the input and ph:name to the server's name. Offline leaves the local name as typed. Cost if wrong: a rename that fails offline shows a generic message.
Task 11: accepted with no test: auth.ts and useSession have no unit test, because the project has no React testing library and a test built on a mocked Google widget plus a mocked DOM would assert nothing. Honest gap recorded rather than a test that exists to look tested.
Task 11: UNVERIFIED and blocking on Moritz: the entire signed-in path (groups link, sign out, delete account, rejected-rename toast) has never run, because no Google OAuth client id exists yet.
Task 11: review 1 — spec compliant, 2 Important, 2 minors. Reviewer confirmed all four decisions landed, that nothing reaches Google or the API on first paint without a client id, that .card genuinely no longer exists in theme.css, that the rename revert is scoped to the three name_* codes only, and that useSession does not leak subscriptions.
Task 11: minor (deferred): if the sign-in callback's onDone throws after the session was already written, the toast says sign-in failed although it succeeded. Unreachable today because onDone cannot throw.
Task 11: fix round 1/5 (4 addressed, 0 open; commits b5e8d10..1ca1a86). Re-reviewer confirmed the retry uses the same code path as the mount effect, that loading is reassigned synchronously inside the promise executor so concurrent calls cannot append two script tags, and that writeSession/emit sit unconditionally outside signOut's try.
Task 11: minor (deferred): a failed GSI load leaves its dead script tag in document.head, so repeated retries under a persistent failure accumulate one tag per attempt. Cosmetic.
Task 11: minor (deferred): the mount effect's dependency array omits attemptSignIn, which is redefined each render. Harmless.
Task 11: complete (commits 12d1787..1ca1a86, review clean). The signed-in path remains unverified at runtime and is Moritz's to try once a client id exists.
Cross-session note (hub-9f): clock fix landed on main as ce4f99a. Cause was a pause gap, not a resume error — the Android notification shade and other system overlays do not stop the activity, so neither visibilitychange nor appStateChange fired and the clock kept running behind them (13 seconds of shade cost 13 seconds on an S23). MainActivity now forwards onWindowFocusChanged. Submitted times get shorter by exactly the overlay time; nothing that was genuinely solve time is affected, and the 4-to-20-second floors are far below. Merge ce4f99a into feat/leaderboard before Task 14 touches Play.tsx; the two insertion points (onSolved, the result block) are untouched by their change.
Task 12: implemented DONE_WITH_CONCERNS (commit 1f17cb9, web tests now 10). First task in this feature whose signed-in UI was actually exercised: the implementer built local Worker bindings and a dev-only token script, ran wrangler dev against a local D1, and drove create, join, leave, refusals, offline and dark mode in a real browser.
Task 12: implementer fixed an unguarded race in useGroups on its own — a stale in-flight /groups response could repopulate the list after sign-out — and added the wrangler cache directory to .gitignore, found by actually running the local workflow rather than by reading about it.
Task 12: environment note for Moritz: the local dotenv override file under apps/web could not be created at all, because his own guard-secrets hook blocks writes to that path even for a non-secret value, and even blocks a shell command that merely names it. VITE_API_BASE was passed as a shell variable instead. If he wants that file locally he has to create it himself or relax the hook.
Controller: merged main (ce4f99a, the clock fix) into feat/leaderboard while the worktree was quiet. Suite green afterwards: core 134, web 10, api 54.
Task 12: review 1 — spec compliant, 1 Important, 5 minors. Reviewer verified the refusal mapping against the API source, that the race fix drops only responses whose session actually changed, that the dev script is excluded from both the Worker bundle and the typecheck, and that the gitignore entries actually match the files on disk (git check-ignore, not just reading).
Task 12: Ruling: the first of the three new tests is deleted rather than kept. It re-encodes the same code-to-message pairs the module already holds, so it can only fail if someone makes the identical typo twice. The other two encode real invariants (two distinct 403s must not collapse; an unknown code must not crash). A test that cannot fail for a real reason costs attention and buys nothing.
Task 12: minor (deferred): useGroups guards sign-out but not unmount; harmless under React 19 and a guard would be noise.
Task 12: minor (deferred): Friends() does not consume useGroups' loading flag, so first paint cannot distinguish fetching from zero groups. Task 15 is the intended consumer.
Task 12: fix round 1/5 (4 addressed, 0 open; commits 1f17cb9..ab3f4fa, isolated from the main merge in between). Re-reviewer confirmed the two forms are independent, both flags clear in finally so a thrown ApiError cannot strand a disabled button, and judged the implementer's reasoning about same-tick clicks sound: real input dispatches each tap as its own task, and the implementer's own test pins the boundary by showing a forced same-tick double dispatch is the only way to get two requests.
Task 12: complete (commits 1ca1a86..ab3f4fa, review clean)
Task 13: implemented (commit 3d39f6b, web tests now 11). Verified live against wrangler dev with real dev sessions: prefilled one-tap join, unknown code, signed-out, already-member, Invite to clipboard with the exact text, both themes.
Task 13: deferred for the final review / deploy: apps/api/src/http.ts hardcodes http://localhost:5173 in the CORS allowlist, so a Vite dev server that moves to another port is silently blocked. It cost the implementer time during verification. Allowing any localhost port would be low risk, since CORS is not what protects the session token, but it is a deliberate change to make rather than a drive-by.
Task 13: review 1 — spec compliant, 1 Important, 2 minors. Controller adjudicated the one point where report and review contradicted each other: the review is right. theme.css:221 sets .row-card.friends-row to two columns and Friends.tsx now puts three direct children in it, so Leave wraps to an implicit second row under the text. The implementer's claim of visual verification in both themes cannot have covered this row.
Task 13: minor (deferred): the first joinUrl test builds its expectation with the same expression the implementation uses, so it only catches an unconditional swap to SHARE_ORIGIN. The second test pins a hardcoded literal and is the one that would have caught the historical WebView bug.
Task 13: fix round 1/5 (2 addressed, 0 open; commits 3d39f6b..387e62b). Re-reviewer confirmed the wrapper restores exactly two grid children, that its 14px gap reuses the card's own existing value rather than inventing one, and that the trim now runs before the slice, which also fixes a leading space eating the last real character. It also separated the load-bearing evidence (equal getBoundingClientRect tops prove a shared row; a 409 already-a-member proves the trimmed code matched server-side) from the decorative parts.
Task 13: minor (deferred): no regression test for the leading-space bug specifically; it was verified in the live DOM instead.
Task 13: complete (commits ab3f4fa..387e62b, review clean)
Task 14: implemented DONE_WITH_CONCERNS (commit 8639382, web tests 17). Implementer found and fixed two real bugs in my reference queue.ts: an unbounded retry loop when a result cannot be matched to a queued entry (the exact risk carried forward from Task 7), and a results.filter outside the try/catch that could reject flush's promise and break its own never-throws contract.
Task 14: Ruling: queued scores get stamped with the player id that was signed in when they were enqueued, and flush submits only entries whose id is undefined or matches the current player; others are skipped and left in place. Reason: without it, a sign-out followed by a different sign-in submits the first player's results under the second player's name. This app is built for a household with shared devices, so that is the expected case, not an exotic one, and the result would be a fabricated entry in a friends leaderboard that nobody involved could see. Entries with no id still go to whoever signs in next, which preserves the ordinary play-first-sign-in-later path. Cost if wrong: an entry whose owner never returns sits in the queue until the cap of 100 drops it.
Task 14: verification note: this implementer had no browser automation available, unlike Tasks 12 and 13. It drove real offline and online transitions against wrangler dev instead and proved the entry reached D1 by resubmitting for a duplicate status — evidence about the mechanism rather than the pixels, which is the stronger check here.
Cross-session note (hub-9f): main is now still at 9ec0582 and that session has stopped working there. Already merged into this branch: ce4f99a. Still to merge: 9ec0582 (versionCode 8 plus release notes 8; Release 8 is built, signed and uploaded to the closed test). Merge it once Task 14's implementer is done in the worktree. If the leaderboard ever needs its own test build, it starts at versionCode 9.
Task 14: review 1 — spec compliant on the interface, 2 Important, 3 minors. Reviewer verified the batch cap, the status set and the dedup scoping against the server's actual code rather than the brief, and confirmed the ownership filter runs before batching so a departed player's entries never occupy a slot or affect the stuck-loop guard. Controller checked both commit bodies and file scopes directly.
Task 14: fix round 1/5 (5 addressed, 0 open; commits cfbdba9..0938224). Re-reviewer traced the type guard against every shape that could have thrown, confirmed the new trim rule can never drop a current-player entry while any other-player entry survives (the fallback slice only runs once the kept array is entirely the current player's), and judged the throttled test stronger than asked: asserting the cumulative request count stays at one across two flushes proves the loop stopped rather than merely that the entry persisted.
Task 14: minor (deferred): isValidEntry does not reject NaN in the numeric fields. It is a type check, not a sanity check, and the server's plausibility floor catches nonsense anyway.
Task 14: complete (commits 387e62b..0938224, review clean)
Task 15: implemented (commit 3c7bc4b, web tests 27). Implementer caught three bugs in the brief before they shipped, the worst being that the brief's .board class collides with the existing SVG puzzle-board class — it would have restyled the game boards themselves.
Task 15: review 1 — spec compliant, 1 Important (plan-mandated), 2 minors. Reviewer independently verified the class collision, the Daily row's grid child count against the template, the percentile arithmetic by hand at 20, 21 and 1, and the report idempotency against the server source rather than the report.
Task 15: fix round 1/5 (2 addressed, 0 open; commits 3c7bc4b..8e91208). Re-reviewer walked two and three overlapping requests in both resolve orders and confirmed only the most recently fired one can land, that setLoading and the error path use the same guard, and that a refetch back to a previous puzzle still works.
Task 15: minor (deferred): two requests sharing an identical group-and-puzzle key can still race each other, so the later-resolving one wins. Always the correct puzzle under the correct heading, only possibly slightly stale.
Task 15: minor (deferred): if useBoard's groupId ever went from a string to null mid-flight, loading could stick true. Unreachable today — the prop is non-nullable, the component unmounts instead, and the other consumer never reads loading.
Task 15: minor (deferred): the race guard has no unit test. It sits inside useEffect/useRef with no pure-function seam, and this suite deliberately has no React testing library. Recorded honestly rather than given a shape that exists only to be testable.
Task 15: complete (commits 0938224..8e91208, review clean)
ALL FIFTEEN IMPLEMENTATION TASKS COMPLETE. Task 16 (deploy) is deliberately not dispatched, per the pre-flight ruling.
FINAL REVIEW (opus, whole branch 9ec0582...8e91208): "Merge after fixes". 1 Critical, 4 Important, 15 minors, plus a triage of every deferred item and a verdict on every ruling.
Final: Ruling: resetProgress keeps leaving ph:queue alone (finding 13). Reason: the queue holds results the player genuinely earned, often while offline, and dropping them to honour a local reset destroys real data to avoid mild surprise. A time landing on the board after a reset is odd but true. Documented rather than changed. Cost if wrong: a player who resets sees old times appear on the leaderboard afterwards.
Final: Ruling: the missing API deploy workflow (second half of Critical 1) is not fixed here. It belongs to Task 16, which I deliberately did not dispatch because it creates live resources. Recorded in the hand-off instead.
Final: deferred (accepted): an offline rename diverges until the player edits it again online (finding 14). A rename queue is a feature, not a fix.
Final fix wave re-review (opus): all 15 findings and both requested tests ADDRESSED, but the wave introduced one new Important — deploy.yml passes vars.VITE_API_BASE unguarded, which is "" when the repo variable is unset, and api.ts uses ?? which does not catch "". API_BASE would become '' and every call would hit the Pages origin and fail. It triggers the moment Moritz sets VITE_GOOGLE_CLIENT_ID without also setting VITE_API_BASE, which is the likelier half of the rollout.
Final: Ruling: that one is fixed rather than surfaced, despite the no-second-wave rule. Reason: it is breakage this wave created, it is one line, and leaving it would hand Moritz a feature that breaks precisely when he configures it. The rule exists to stop endless cycles, not to ship a known break. Also folding in the residual of I4 (the Profile name field stays stale until remount), since that is the visible half of the finding I4 was supposed to close. Cost if wrong: one more small dispatch.
